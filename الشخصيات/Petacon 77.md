# معلومات حول الشخصية:
- ...


# 🤖 ميكانيكيات اللاعب - Petacon

## 🧠 مبدأ التصميم العام:

> "كل حركة وقرار تتخذه كلاعب يجب أن يكون له وزن، إما يكافئك أو يعاقبك." — هذه القاعدة هي جوهر الإدمان والمتعة.

---

## 🎮 المهارات الأساسية (مع تفاصيل دقيقة):

| المهارة        | الزر       | الطاقة المستهلكة | الدالة المستخدمة             | ملاحظات تقنية                                                                                  |
| -------------- | ---------- | ---------------- | ---------------------------- | ---------------------------------------------------------------------------------------------- |
| **المشي**      | ← →        | 0%               | `_physics_process()`         | السرعة تحدد بـ `walk_speed` (مثلاً: `120.0`)                                                   |
| **القفز**      | Space      | 0%               | `velocity.y = -jump_force`   | يمكن ضبط `jump_force` بـ `300.0`. يستخدم فقط إذا `is_on_floor()` = true                        |
| **قفز مزدوج**  | Space ×2   | 15%              | `can_double_jump` + شرط طاقة | عند التنفيذ: `velocity.y = -double_jump_force`, ويتم تعطيل `can_double_jump = false`           |
| **Dash**       | Shift      | 20%              | `dash_vector`, `lerp()`      | dash أفقي لمدة `0.2 ثانية` بسرعة `dash_speed = 350` مع `invincibility_frames = true` خلال dash |
| **ليزر مستمر** | Mouse Hold | 10%/ثانية        | `fire_laser()`               | لا يُسمح بالإطلاق إن كانت الطاقة < 10%. يستخدم `RayCast2D` أو `Area2D` للكشف عن التصادم        |
| **صاروخ موجه** | Key: X     | ذخيرة Nyxel      | `launch_missile()`           | ينفذ إذا توفرت ذخيرة Nyxel. يتم استخدام `seek()` أو `look_at()` لتتبع العدو                    |

---

## ⚡ نظام الطاقة:

- الطاقة القصوى: `100`
    
- معدل الشحن: `+10%/ثانية`
    
- لا تشحن إذا:
    
    - مستوى الإشعاع ≥ 45%
        
    - اللاعب في حالة Overheat
        

> ⚠️ **Overheat Mode:** إذا تم استهلاك الطاقة بالكامل، يدخل اللاعب في حالة تعطيل كامل لمدة `3 ثواني`

---

## 🛡️ الذرع (درع الطاقة):

- يمثل صحة اللاعب:
    
    - `max_shield = 100`
        
    - يتناقص بالضربات، وتظهر ومضات بصرية عند الضرر
        
- يمكن ترقية الدرع عبر Pickups أو ترقيات مستقبلية (مثلاً: +20 درع)
    
- يُعرض في HUD على شكل ProgressBar
    

---

## 🧩 ميكانيكيات إدمانية متقدمة:

### 🔄 Overcharge Mode:

- إذا بقيت الطاقة ممتلئة بالكامل لأكثر من `5 ثواني`
    
- اللاعب يدخل وضع Overcharge:
    
    - `+25% سرعة`
        
    - `+50% ضرر`
        
    - تأثيرات بصرية خاصة
        
    - ينتهي إذا: فقد الطاقة أو أخذ ضرر
        

### 🌀 Clutch Dash:

- إذا فعل Dash قبل 0.2 ثانية من الهجوم العدو (timed)
    
- يتم تفعيل Dash خاص:
    
    - زمن بطيء لحظي (`Engine.time_scale = 0.5 لمدة 0.3 ث`)
        
    - Dash أقوى وأطول بـ 20%
        

### 📉 Energy Drain Field:

- مهارة مستقبلية: عند التفعيل تسحب طاقة من الأعداء القريبين داخل `Radius = 150`
    
- تُستعمل كل `30 ثانية`
    
- تعطي +10% طاقة لكل عدو متأثر
    

### 🎯 تحديات تلقائية:

- يتم توليدها عشوائيًا:
    
    - "اقضِ على 3 أعداء دون لمس الأرض"
        
    - "لا تستخدم Dash لمدة 20 ثانية"
        
- يكافئ اللاعب بـ loot أو طاقة أو نقطة تحسين
    

---

## 🎲 عشوائيات ذكية:

- عند إطلاق الصاروخ، نسبة خطأ 5–10% (random offset)
    
- Nyxel يعطي loot بشكل عشوائي كل 10–30 ثانية
    
- بعض المهارات تتغير فعاليتها حسب مستوى الإشعاع
    

---

## 🚫 حالات منع استخدام المهارات:

|المهارة|لا تُستخدم إذا:|
|---|---|
|قفز مزدوج|لا طاقة كافية / تم استخدامها بالفعل|
|Dash|لا طاقة / تحت تأثير Overheat|
|ليزر|الطاقة < 10% / تم تعطيله من بيئة مشعة|
|صاروخ موجه|لا ذخيرة من Nyxel / العدو خارج الرؤية|

---

## 🛰️ المساعد Nyxel - (سيُوضع لاحقًا في ملف منفصل):

> هنا فقط نضع التذكير أن المساعد:

- يتفاعل بشكل مستقل
    
- يرمي Loot وذخيرة
    
- يطلق صواريخ صغيرة كل مدة
    
- يُبرمج لاحقًا داخل ملف خاص
    

---

## 🧠 ملاحظات عامة:

- كل ميكانيكية مربوطة بالطاقة لتعزيز التفكير التكتيكي
    
- يجب تشجيع اللاعب على الحركة الذكية لا العشوائية
    
- التصميم يحفز على التوقيت + إدارة الموارد + التجربة المتكررة